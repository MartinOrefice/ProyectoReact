import { createTheme } from "@mui/material";

const theme = createTheme ({
    palette: {
        primary:{
            main:'#d50000'
        },
        secondary:{
            main:'#fafafa'
        }
    }
})

export default theme